
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: networkdevices.ndd.henderiw.be
spec:
  group: ndd.henderiw.be
  names:
    kind: NetworkDevice
    listKind: NetworkDeviceList
    plural: networkdevices
    singular: networkdevice
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Discovery status
      jsonPath: .status.discoveryStatus
      name: discoveryStatus
      type: string
    - description: Kind of device
      jsonPath: .status.deviceDetails.kind
      name: Kind
      type: string
    - description: SW version of the device
      jsonPath: .status.deviceDetails.swVersion
      name: SwVersion
      type: string
    - description: macAddress of the device
      jsonPath: .status.deviceDetails.macAddress
      name: MacAddress
      type: string
    - description: serialNumber of the device
      jsonPath: .status.deviceDetails.serialNumber
      name: serialNumber
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: NetworkDevice is the Schema for the networkdevices API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: NetworkDeviceSpec defines the desired state of NetworkDevice
            properties:
              address:
                description: Address defines how we connect to the network node
                type: string
            type: object
          status:
            description: NetworkDeviceStatus defines the observed state of NetworkDevice
            properties:
              deviceDetails:
                description: The discovered DeviceDetails
                properties:
                  hostname:
                    description: Host name
                    type: string
                  kind:
                    description: the Kind of hardware
                    type: string
                  macAddress:
                    description: the Mac address of the hardware
                    type: string
                  serialNumber:
                    description: the Serial Number of the hardware
                    type: string
                  swVersion:
                    description: SW version
                    type: string
                type: object
              discoveryStatus:
                default: Not Ready
                description: DiscoveryStatus holds the discovery status of the networkNode
                enum:
                - ""
                - Ready
                - Not Ready
                - Discovery
                type: string
              lastUpdated:
                description: LastUpdated identifies when this status was last observed.
                format: date-time
                type: string
            required:
            - discoveryStatus
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
